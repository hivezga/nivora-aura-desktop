# Compiled output
/dist
/dist-ssr
/node_modules
/src-tauri/target
/src-tauri/Cargo.lock

# IDE and OS files
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db
desktop.ini
*~

# Wrong package manager lockfiles (project uses pnpm)
package-lock.json
yarn.lock

# Local environment files
.env
.env.local
.env.*.local
*.local

# Tauri files
/src-tauri/gen/
/src-tauri/tauri.conf.json.bk

# Security - certificates and private keys
*.pem
*.key
*.p12
*.pfx
*.keystore

# Test coverage
coverage/
.nyc_output/

# Log files
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Models - These are too large for a git repository
# and should be downloaded by the user as per the README.
/src-tauri/models/
/src-tauri/resources/
~/.local/share/nivora-aura/

# Ollama models are tracked via Git LFS (1.6GB - under 2GB limit)
# Bundled for 100% offline capability - no separate download required

# Build artifacts (optional - can be distributed via GitHub Releases)
src-tauri/target/release/bundle/*.deb
src-tauri/target/release/bundle/*.rpm
src-tauri/target/release/bundle/*.dmg
src-tauri/target/release/bundle/*.msi
src-tauri/target/release/bundle/*.AppImage
