# Ollama binaries (models excluded - too large for GitHub LFS 2GB limit)
resources/ollama/bin/ollama-* filter=lfs diff=lfs merge=lfs -text

# Piper resources
resources/piper/voices/*.onnx filter=lfs diff=lfs merge=lfs -text
resources/piper/bin/piper-* filter=lfs diff=lfs merge=lfs -text
resources/piper/bin/*.so* filter=lfs diff=lfs merge=lfs -text

# Whisper models (if added later)
*.bin filter=lfs diff=lfs merge=lfs -text
